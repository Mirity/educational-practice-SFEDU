{% layout "layout/default-layout.liquid", title: 'Clients' %}
{% block %}
    {% assign clients = this.getData %}

    {% for client in clients %}
        {% include 'samples/client-sample.liquid', client %}
    {% endfor %}
{% endblock %}